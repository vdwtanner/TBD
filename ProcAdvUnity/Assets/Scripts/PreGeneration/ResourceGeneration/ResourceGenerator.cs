using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResourceGenerator {
    private int m_numNodes;

    ResourceGenerator(int numNodes)
    {
        m_numNodes = numNodes;
    }
}
